# Copy to terraform.tfvars (not committed) and edit if needed.
# aws_profile       = "my-aws-project"   # optional; omit or "" for default/AWS_PROFILE
# aws_region        = "ap-south-1"
# s3_bucket_name    = "asset-register-backup-bigb787"
# public_key_path   = "../asset-register.pub"
