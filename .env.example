# AWS (local dev with named profile; on EC2 use IAM role and region only)
AWS_PROFILE=my-aws-project
AWS_REGION=us-east-1
S3_BUCKET_NAME=asset-register-backup-bigb787

# App
DATABASE_PATH=data/assets.db
APP_HOST=0.0.0.0
APP_PORT=8000
ENVIRONMENT=production
SECRET_KEY=change-this-to-a-random-string
